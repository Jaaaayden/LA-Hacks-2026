ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
MONGODB_URI=

# Fetch.ai Agentverse — agent identity seeds (any random string, but keep STABLE
# once registered on Agentverse — changing the seed changes the agent address)
COORDINATOR_SEED=
SCOUT_SEED=
PRICER_SEED=
# Mock seller agent that auto-commits the demo Payment Protocol cycle.
# Any random string; keep stable once Agentverse has registered the agent.
PAYMENT_SINK_SEED=

# Get from https://agentverse.ai → Settings → API Keys
AGENTVERSE_API_KEY=
